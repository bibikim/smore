<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.smore.mapper.RedMapper">
	
	<!-- 
	<select id="selectAllRedCnt" resultType="int">
		SELECT COUNT(*)
		  FROM GRP_REDBELL
	</select>
	 -->

	<!-- 
	<select id="selectAllRedList" parameterType="Map" resultType="StudyGroupDTO">
		SELECT A.ROW_NUM, A.RED_NO, A.ID, A.STUD_NO, A.RED_CONTENT, A.RED_DATE, A.USER_NO
		  FROM (SELECT ROW_NUMBER() OVER(ORDER BY RED_NO DESC) AS ROW_NUM, R.ID, R.STUD_NO, R.RED_CONTENT, R.RED_DATE, R.USER_NO
		          FROM GRP_REDBELL B) A
		 WHERE A.ROW_NUM BETWEEN #{begin} AND #{end}
	</select>
	 -->
			
	<insert id="insertRed" parameterType="GrpRedbellDTO">
		INSERT INTO GRP_REDBELL
			(RED_NO, ID, STUD_NO, RED_CONTENT, RED_DATE, NICKNAME)
		VALUES
			(#{redNo}, #{id}, #{studNo}, #{redContent}, #{redDate}, #{study.nickname})	
	</insert>
	
	
</mapper>

	
	
	